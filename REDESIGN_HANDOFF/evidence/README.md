# Evidence notes

`LATEST_EXPLAIN_LIKE_10_LOCAL_VERIFICATION.md` is the newest attached local run available to this handoff. It proves the real iOS simulator Standard / Even simpler / Show an example journey and records the current persistence-test failure.

`LEGACY_SCREEN_FLOW_CONTEXT.md` is useful for existing screen inventory, but parts of its old `no generative-AI dependency` policy are superseded by `00_READ_ME_FIRST.md` and `01_CURRENT_VERIFIED_STATE.md`.

`P0_CONTENT_INTEGRITY_HISTORY.md` explains why extraction/source integrity and real-model verification became hard requirements.
