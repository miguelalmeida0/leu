# LEU V27 INTELLIGENCE LEAP — certification OPEN

This is implemented code with partial executable evidence, **not a completed product certification**. No commit or push. Work is isolated on `feature/leu-v27-intelligence` in `LeuV27Intelligence`. A SHA-256 comparison of 23,315 canonical files found zero changes to `LeuNightFieldContrast`.

## 1. WHAT CHANGED

Reader actions now expose source-bound Teach Leu, applicable deterministic labs, admitted questions and verified connections. New SwiftUI sheets reuse Night Field, the existing Reader, learning repository, BM25 index and Apple provider. An independent bundle identifier (`dev.shelf.v27`) keeps this app's containers separate from canonical physical-device acceptance data.

**BEFORE V27 → AFTER V27 (implemented and host-tested; UI execution pending)**

- Reading about keys → explain “Keys tell React which item is which when a list changes.” The comparison recognizes the identity point while retaining the complete stability/sibling-scope assertion as worth adding.
- Reading about list identity → move C before A/B; stable IDs move state `[7,0,0]` to `[0,7,0]`, while position keys retain `[7,0,0]` and transfer which item occupies that state.
- Reading about stale caches → create a cached copy of 1, change the original to 2, observe reuse still returning 1, then recompute to 2.
- Leaving a typed explanation → a single Resume affordance restores that actual unresolved explanation; a changed/unverified source cannot resume an old comparison.

## 2. QUESTION V3

The existing on-device provider selects a closed claim/operation ID. V3 realizes source-bound questions and finite counterstatements of nearby admissible claims. Correct answers preserve canonical PDF text and exact UTF-16 ranges. Counterstatements are wrong answer options, never admitted technical facts. Independent admission recompiles the entire candidate and rechecks persisted V3 questions.

Final checks reject generic/malformed stems, missing subject articles, instructions, mnemonics, furniture, imbalanced options and near-duplicate options. Integrity-unknown and degraded pages produce zero V3 claims. Deterministic fallback carries `backend=deterministic-v3`, `modelInferencePerformed=false`, and no sampling/inference configuration; model diagnostics exclude it.

Two real React p.3 questions were manually inspected after production extraction:

1. **Which effect explains the role of a stable key?** Correct: “A stable key helps React match an item to its previous\ninstance within a list of siblings.” Wrong options preserve explicit controlled reversal witnesses: “Reordering should make one item inherit the local state of another”; “A freshly generated random key also does not destroy continuity between renders.”
2. **Which restriction applies to reordering?** Correct: “Reordering should\nnot make one item inherit the local state of another.” Wrong options reverse the stable-key matching and random-key continuity assertions.

The `\n` notation above denotes actual canonical PDF line breaks. See `evidence/React Notes-p3-accepted.json` for exact values and ranges. These remain fairly conservative relation questions, not evidence of broad excellent-language generation. Full cognitive-operation/corpus coverage is not certified.

## 3. TEACH LEU

Implemented editor, comparison, exact-source return, persistence and resume. Apple structured generation proposes learner/source ID pairs; no free-form model verdict is shown. Deterministic proofs support exact assertions, literal polarity reversal, and a narrow audited identity paraphrase. Other paraphrases remain **“Your source doesn't settle this.”** This is deliberately conservative and does not provide general semantic entailment, grades or mastery diagnosis.

Production PDF replay verifies the requested React explanation captures identity, retains omissions and cites p.3. New iOS journey remains unexecuted.

## 4. CONNECTIONS / RABBIT HOLES

Existing local BM25 retrieves candidates. Admission requires the same explicit predicate/object/qualifier/polarity in two distinct, verified sources; identical evidence and near-identical passages are rejected. Reader, Lens/concept, question feedback and Teach results expose the action only when admitted connections exist. Both excerpts, exact PDF return and bounded four-step chains are implemented.

Synthetic valid/invalid source-pair tests pass. The three bundled PDFs produced **zero admitted cross-document connections**. No fabricated relationship or sample substitution was inserted. Positive real-library/UI certification remains open.

## 5. TRY IT

Two production PDF bindings pass host verification: React stable/index keys and System Design cached-copy staleness. Source evidence does not support HTTP/ETag/TTL simulation, so the cache lab makes no protocol claim. Transitions, state, reset and outcomes are deterministic. Confirmed preserved identity uses green checks inside circles; other indicators remain neutral. Exact full-page evidence is shown when activity prerequisites extend beyond a selected paragraph.

## 6. UNDERSTANDING MEMORY

UnderstandingAttempt and UnderstandingEvent reuse LearningSnapshot/LearningRepository. Draft writes coalesce while pending. Validated comparisons persist; stale source writes are rejected, history is retained, and schema-1 snapshots without the new fields decode with empty collections. Current-source checks exclude stale thoughts from Resume.

Source comparisons and JSON round-trip pass. **Actual FileLearningSnapshotStore reopen is blocked**: this session denies its atomic `learning.next.json` write. The test remains strict and failing; production storage was not weakened.

## 7. REAL-PDF RESULTS

Production PDFKit extraction, geometry integrity and canonical range replay ran across all 12 pages of React Notes, System Design and JavaScript Deep Dive. Two React questions admitted; no questions admitted from the other two books under V3's conservative claim/choice contract. Both required lab sources are representable. This limited yield is a remaining intelligence-quality gap.

`javascript_midlevel_interview_mobile_mastery.pdf` was not located in Downloads, Documents, Desktop, or available CloudStorage/Mobile Documents. `field_manual` was not substituted. **20-question inspection and 10-question acceptance certification remain OPEN.**

## 8. INTELLIGENCE QUALITY FAILURES FOUND

- “Explain why…” could enter claim extraction: fixed by excluding imperative prose.
- Identical quotations across documents could appear as new connections: fixed at evidence and passage boundaries.
- The canonical cache trade-off sentence was not representable: added a finite `avoids …, but …` relation without removing either clause.
- A V3 stem omitted the article in “of stable key”: fixed by retaining the canonical subject phrase. A case-insensitive uppercase-article check then falsely rejected the corrected phrase; fixed with a case-sensitive subgroup and regression test.
- Deterministic provenance inherited Apple sampling settings and diagnostic counts: separated explicit no-inference provenance and counts.
- A V2 downgrade test expected semantic rejection after removing selection proof. V3 counterstatements correctly fail earlier at `unsupportedChoices`; updated that exact expected rejection, preserving failure coverage.

## 9. PERFORMANCE

See `evidence/real-pdf-report.json` for measured values. Host measurements: approximately 5.5 ms source admission; 0.34 ms warm retrieval over the three real sample PDFs. A **synthetic** 51-document repeated-sample benchmark measured roughly 3.2 ms median retrieval, with a 1.18-second cold index build performed off the main actor. This is not a real 51-document library or an iPhone benchmark.

Actual reader action presentation under 100 ms is unverified. Teach comparison coalesces requests, cancels stale/closed-sheet inference, bounds response generation and reuses persisted valid results. No successful model latency was measured.

## 10. TEST RESULTS

- Complete ShelfCore host compile: PASS.
- Focused XCTest: **31/32 PASS, 0 skipped**. Includes 48 representative technical source cases inside one test. The sole failure is the denied real file-store atomic write.
- Existing grounded admission tests adapted to V3: **18/18 PASS**, included above.
- Production PDF replay: PASS for extraction, two admitted question ranges, both lab bindings and the requested Teach comparison.
- Apple question/Teach provider host compile: PASS.
- Actual host model call: availability `available`, generation failed with FoundationModels/ModelManagerServices error **1008**. **No successful inference, accepted model output or persisted model question proven.**
- App and new UI-test syntax: PASS; this is not a SwiftUI typecheck/build.
- iOS core/voice module compile: PASS in the initial attempt. Whole app module compilation blocked by `sandbox-exec: sandbox_apply: Operation not permitted` while expanding Observation macros.
- Native Xcode execution blocked by CoreSimulator POSIX 61 and SwiftPM temporary-lock permissions. Four new UI journeys are present but **0 executed**. Historical 74-test and other broad gates were not run.

## 11. SCREENSHOTS / EVIDENCE

**No new iOS UI screenshots were produced.** These are real PDF reference renders only:

- `evidence/React Notes-p3-PDF-reference.png`
- `evidence/System Design-p3-PDF-reference.png`
- `evidence/JavaScript Deep Dive-p3-PDF-reference.png`

Executable evidence: `deterministic-tests.log`, `real-pdf-report.json`, per-page `*-analysis.json` / `*-proposals.json` / `*-accepted.json`, `mac-model-probe.json`, `native-attempt.log`, `ios-module-Shelf.log`, `app-syntax.log`. The native runner exports real XCTest attachments when execution becomes available.

## 12. REMAINING RISKS

1. Entire native build/UI visual and interaction certification remains open; code is not claimed production-ready.
2. Actual file-store persistence/reopen test must run outside the current denied-write environment.
3. Real model execution failed despite availability; no successful V27 model result has been certified on iOS or host.
4. Missing mobile_mastery corpus; 20 inspected / 10 accepted target unmet.
5. Low representability/choice yield on System Design/JavaScript; operation coverage and question language need real-corpus evaluation.
6. Teach comparison admits only narrow proven language; many valid paraphrases remain unsettled.
7. Positive real-library connection and source-to-source UI journey remain unverified.
8. Small-screen/AX layout, live latency, full application typechecking and packaged resources require the pending native run.

## 13. FILES CHANGED

See `change-manifest.json` for exact paths: 19 modified existing files and 21 added implementation/test/script files. All are in the sibling checkout. The canonical baseline contains 23,315 unchanged files. No commit or push.

## 14. EXACT COMMAND TO RUN LOCALLY

```bash
cd /Users/malmeida/Documents/ChatGPT/Leu/LeuV27Intelligence
bash scripts/verify-v27.sh
```

Pass the actual mobile_mastery PDF's absolute path as the first argument when located. The runner fails at failing gates, uses only V27/related focused tests, runs four new UI journeys, and exports attachments. Without the PDF it explicitly leaves final certification open. It does not erase the simulator or alter the canonical checkout.
